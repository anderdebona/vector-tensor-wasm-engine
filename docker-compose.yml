version: '3.8'

services:
  vector-wasm-engine:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3009:3009"
    environment:
      - PORT=3009
      - NODE_ENV=production
    restart: unless-stopped
